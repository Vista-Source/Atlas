namespace {{namespace}} 
{
    {{ for enum in enums }}
    public enum {{ enum.name }} : {{ enum.underlying_type }}
    {
        {{ for value in enum.values }}
        {{ value.name }} = {{ value.value }},
        {{ end }}
    }
    {{ end }}
}
